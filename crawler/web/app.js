const $=s=>document.querySelector(s);["limit","workers"].forEach(x=>$("#"+x).oninput=e=>$("#"+x+"-val").textContent=e.target.value);
$("#crawl").onsubmit=async e=>{e.preventDefault();const button=e.currentTarget.querySelector("button"),url=$("#url").value;button.disabled=true;button.textContent="FLEET IN FLIGHT...";$("#mission").hidden=false;$("#results").innerHTML="";$("#target").textContent=url;$("#state span").innerHTML="CRAWLING<small>GOROUTINES IN FLIGHT</small>";$("#mission").scrollIntoView({behavior:"smooth"});try{const r=await fetch("/api/crawl",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url,maxPages:+$("#limit").value,concurrency:+$("#workers").value})});if(!r.ok)throw Error(await r.text());const data=await r.json();show(data.pages)}catch(err){$("#error").textContent=err.message;$("#error").style.display="block";setTimeout(()=>$("#error").style.display="none",3000)}finally{button.disabled=false;button.innerHTML="LAUNCH MISSION <span>↗</span>"}};
function show(items){$("#state span").innerHTML="MISSION COMPLETE<small>ALL WORKERS RETURNED</small>";let i=0,t=setInterval(()=>{if(i>=items.length){clearInterval(t);return}const r=items[i++],el=document.createElement("div");el.className="result "+(r.status>=400||r.error?"bad":"");el.innerHTML=`<span class="code">${r.status||"ERR"}</span><div class="resource"><b>${esc(r.title||r.error||"Unreachable")}</b><small>${esc(r.url)}</small></div><span>${r.links}</span><span>${r.durationMs}ms</span>`;$("#results").append(el);metrics(items.slice(0,i))},55)}
function metrics(a){$("#pages").textContent=a.length;$("#success").textContent=Math.round(a.filter(x=>x.status<400&&x.status>0).length/a.length*100)+"%";$("#speed").textContent=Math.round(a.reduce((n,x)=>n+x.durationMs,0)/a.length)+"ms";$("#links").textContent=a.reduce((n,x)=>n+x.links,0)}function esc(s){const d=document.createElement("div");d.textContent=s;return d.innerHTML}
